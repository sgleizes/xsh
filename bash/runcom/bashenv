#
# This file is sourced by `bash` non-interactive shells.
# It is the only startup file read in such cases.
#

# Source xsh or error out silently.
source "${XSH_DIR:-${XDG_CONFIG_HOME:-$HOME/.config}/xsh}/xsh.sh" 2>/dev/null ||
  return 1

xsh runcom env
